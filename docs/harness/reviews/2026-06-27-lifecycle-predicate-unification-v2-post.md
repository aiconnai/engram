REVIEW_VERDICT: FAIL prior canvas/docs/single-writer issues are mostly fixed and targeted checks pass, but referenced review evidence is missing and compression apply paths can duplicate summaries indefinitely.

- [BLOCKER] Missing referenced review evidence artifact. `docs/harness/progress/2026-05-30-harness-bootstrap.md:1491-1492` points to the lifecycle plan; that plan claims Codex v3 PASS evidence at `docs/harness/reviews/2026-06-27-lifecycle-predicate-unification-codex-v3.md` (`docs/superpowers/plans/2026-06-27-lifecycle-predicate-unification.md:13`), and the chair note also lists it as retained valid evidence (`...chair-v3.md:6-24`). The file does not exist, so the progress/plan review-evidence chain is not independently verifiable.

- [HIGH] Compression paths are now non-idempotent and can repeatedly create duplicate summaries for the same archived memory. `memory_archive_old` filters to Archived rows (`src/mcp/handlers/summarize.rs:261-270`) then creates a summary with `summary_of_id` (`:316-324`) without excluding rows already summarized or marking them compressed. `compress_old_memories` has the same pattern (`src/storage/queries/retention.rs:232-239`, `:296-304`), and the server scheduler calls it repeatedly (`src/bin/server.rs:746-748`). Existing tests prove only first compression, not repeat-run safety.

Checked clean areas: review canvas now exists with approaches/hot-path/edge/risk sections; README/ROADMAP no longer assign lifecycle ownership to salience/archive-old; schema remains 44; MCP docs are regenerated; direct grep found no lifecycle writes in salience decay or memory_decay. I ran targeted lifecycle/memory_policy/summarize tests, MCP reference check, cargo check, and diff check successfully.
