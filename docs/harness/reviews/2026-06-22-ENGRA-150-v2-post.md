PASS — ENGRA-150 routes Dream/lifecycle handler transitions through a new query-layer helper with full version/event/sync bookkeeping; no scope creep, no security boundary drift, no gate weakening.

- [LOW] Minor consistency: `update_memory_lifecycle_state` (src/storage/queries/lifecycle.rs) does its work via raw `conn.execute` rather than reusing the canonical `update_memory` path, so version-row/event/sync logic is now duplicated in two places — acceptable since the canvas documents why a public-input field was rejected, but the duplicated sync_state/version-insert SQL is a future drift risk. Evidence: the `INSERT INTO memory_versions ...` and `UPDATE sync_state ...` blocks mirror logic that already lives in the main update query.

- [LOW] The new helper reads `valid_to IS NULL` rows only (correct for the documented edge cases), and `memory_set_lifecycle` correctly maps `EngramError::NotFound` back to the preserved `{"error":"Memory not found"}` shape (src/mcp/handlers/lifecycle.rs:239-245), so the wire contract is unchanged. Tests assert event count (==1), version count (==1), and `changed_fields` payload — adequate coverage for the new bookkeeping. MCP schemas untouched, `generate-mcp-reference.sh --check` PASS, canvas present with approaches/edge-cases/breakage table.

Verifico que a mudança é limitada: remove os `UPDATE memories` crus dos handlers, centraliza no query-layer (alinhado com [[handlers-route-writes-through-query-layer]]), e adiciona cobertura de integração para os side-effects de update. Sem execução autônoma, sem mudança de egress, sem importação de pipeline C/C++/ASAN.

REVIEW_VERDICT: PASS ENGRA-150 lifecycle transitions correctly routed through query-layer helper with version/event/sync bookkeeping, tests, canvas, and preserved wire contracts; only minor SQL-duplication drift risk noted.
