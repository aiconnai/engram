# Benchmark Results

This directory keeps small, tracked benchmark snapshots that are useful as
historical evidence. Do not put fresh Criterion output here by default.

Use `target/criterion/` for normal Criterion reports and
`benches/results/generated/` for temporary text exports.
